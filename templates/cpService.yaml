apiVersion: v1
kind: Service
metadata:
  name: cp
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  selector:
    app: rajds-control-plane
  ports:
  - name: grpc
    port: 31314
    targetPort: 31314
  - name: http
    port: 31313
    targetPort: 31313
